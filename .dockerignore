# Git files (not needed in container)
.git/
.gitignore

# GitHub Actions workflows (not needed in container)
.github/

# Node modules (will be installed in container)
node_modules/
npm-debug.log
yarn-error.log

# Build outputs (will be built in container)
.next/
dist/
build/
.mastra/

# Environment files (mounted or provided at runtime)
.env
.env.local
.env*.local

# IDE files
.vscode/
.idea/
*.swp
*.swo

# Logs
*.log

# OS files
.DS_Store
Thumbs.db

# Test files (not needed in production container)
__tests__/
*.test.*
*.spec.*
coverage/
.nyc_output/

# Documentation (not needed in container)
*.md
docs/

# Docker meta-files (not needed in container build context)
Dockerfile*
docker-compose*
.dockerignore
